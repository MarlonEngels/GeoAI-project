# Running the application

To run the application, ensure you have Python installed along with the required dependencies listed in `requirements.txt`. For best results, use a virtual environment. You can install the dependencies using the following pip command:

```bash

pip install -r requirements.txt

```

Then, execute the main application script in the root directory:

```bash

python app.py


```
